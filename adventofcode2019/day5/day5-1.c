#include <stdio.h>#include <stdlib.h>#include <string.h>#include <openssl/md5.h>int main(int argc, char ** argv){  	long long int index = 0, i;	char index_str[32];	unsigned char md5_index[MD5_DIGEST_LENGTH];		//sprintf(index_str, "%lld", index);		//printf("String int: %s\nHash: ", index_str);		//printf("length: %d\n", strlen(index_str) ); 		MD5(md5_index, strlen(index_str), index_str);  		for(i = 0; i < MD5_DIGEST_LENGTH; i++) {    		printf("%0x", md5_index[i]); 		}		printf("\n");++index;		  	const char * door_id = (argc > 1) ? argv[1] : "abc";  	while (0) {		sprintf(index_str, "%lld", index);		printf("%s\n", index_str); 		MD5(md5_index, strlen(index_str), index_str);   		for(i = 0; i < MD5_DIGEST_LENGTH; i++) {    		printf("%02x", md5_index[i]); 		}		++index;  	}  return 0;}